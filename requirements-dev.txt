# ---------------------------------------------------------------------------
# JARVIS - development requirements
#   pip install -r requirements.txt -r requirements-dev.txt
#   python -m pytest tests -q
# ---------------------------------------------------------------------------

pytest>=8.0
httpx>=0.27                          # required by fastapi's TestClient
pyflakes>=3.2                        # dead-import / undefined-name checks
